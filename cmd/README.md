# cmd/

Entry points for Scrinium executables. They appear in M6:

- `cmd/scrinium-fuse` — FUSE mount (build tag `fuse`).
- `cmd/scrinium-webdav` — WebDAV server (build tag `webdav`).

In M0 this directory is empty.
